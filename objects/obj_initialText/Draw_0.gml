


//if (showText)
//{

   // var displayText = "Welcome to the Game! This is an example message that adjusts dynamically.";
  //  var text_margin = 10;
  //  var button_padding = 10;
  //  var button_text = "OK";

    // Calculate text dimensions + padding
  //  var text_width = string_width(displayText) + (text_margin);
  //  var text_height = string_height(displayText) + (2 * text_margin);

    // Button dimensions
    //var button_height = string_height(button_text) + 20; // Padding for text height
  //  var button_width = string_width(button_text) + 40; // Padding for text width

    // Ensure text box is wide enough for the button
  //  if (text_width < button_width + (2 * button_padding)) {
  //      text_width = button_width + (2 * button_padding);
  //  }

    // Adjust text box height for the button
  //  text_height += button_height + button_padding;

    // Text box position
 //   var box_x = camera_get_view_x(view_camera[0]) + 50; // Adjust as needed
  //  var box_y = camera_get_view_y(view_camera[0]) + 300; // Adjust as needed

    // Draw the text box
  //  draw_set_alpha(0.8); // Transparent background
//draw_set_color(make_color_rgb(255, 255, 224)); // Yellowish color
  //  draw_rectangle(box_x, box_y, box_x + text_width, box_y + text_height, false);
  //  draw_set_alpha(1); // Reset transparency for other drawings

    // Draw the text within the text box
   // draw_set_color(c_white);
   // draw_text(box_x + text_margin, box_y + text_margin, displayText);

    // Draw the "OK" button on the bottom right of the text box
   // var button_x = box_x + text_width - button_width - button_padding;
   // var button_y = box_y + text_height - button_height - button_padding;
   // draw_set_color(c_green); // Button color
   // draw_rectangle(button_x, button_y, button_x + button_width, button_y + button_height, false);

    // Center the "OK" text within the button
   // var button_text_x = button_x + (button_width / 2) - (string_width(button_text) / 2);
 //   var button_text_y = button_y + (button_height / 2) - (string_height(button_text) / 2);
  //  draw_set_color(c_white); // Button text color
  //  draw_text(button_text_x, button_text_y, button_text);
//}
